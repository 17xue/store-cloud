description = "store-cloud-serviced-item-api"

dependencies {
    compile(project(":store-cloud-common"))
    compile(project(":store-cloud-common-pojo"))
    compile("org.springframework.cloud:spring-cloud-starter-feign")
}
